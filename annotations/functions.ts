// Função com fat arrow:
const add = (a: number, b: number): number => {
  return a + b;
};

// Função com nome:
function divide(a: number, b: number):number {
  return a / b;
}

// Função anônima atribuída à uma constante:
const multiply = function(a: number, b: number):number {
  return a * b;
}